const Users = require('./users.model');
const Roles = require('./roles.model');
const UserRoles = require('./userRoles.model');
const ParkingSlots = require('./parkingSlots.model');
const ParkingHistory = require('./parkingHistory.model');

module.exports = {
  Users,
  Roles,
  UserRoles,
  ParkingSlots,
  ParkingHistory,
};
